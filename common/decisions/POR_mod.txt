POR_enter_spain = {

	POR_galisia = {

		icon = GFX_decision_category_border_conflicts
		cost = 50

		allowed = {
			always = yes
		}

		available = {
			tag = POR
			has_completed_focus = POR_support_the_spanish_nationalists
			has_war_support >0.5
		}

		visible = { always = yes }

		fire_only_once = yes

		complete_effect = {
			POR = {
				transfer_state = 171
				transfer_state = 783
				transfer_state = 699
				transfer_state = 178
				transfer_state = 297
			}
		}
	}

	POR_unite_iberia = {

		icon = GFX_decision_category_border_conflicts
		cost = 150

		allowed = {
			always = yes
		}

		available = {
			
			date>1939.3.1
			tag = POR
			has_completed_focus = POR_support_the_spanish_nationalists
			has_war_support > 0.5
			
			NOT = {
				is_in_faction_with = SPA
				is_in_faction_with = SPR
			}
		}

		visible = { always = yes }

		fire_only_once = yes

		complete_effect = {
			POR = {
				create_wargoal = {
					target = SPA
					type = annex_everything
				}

				create_wargoal = {
					target = SPR
					type = annex_everything
				}

				every_state = {
					limit = {
						is_core_of = SPA
					}
					add_core_of = POR
				}
				every_state = {
					limit = {
						is_core_of = SPR
					}
						add_core_of = POR
				}
				set_cosmetic_tag = POR_iberian_union
				add_state_core = 118
			}
		}
	}
}
